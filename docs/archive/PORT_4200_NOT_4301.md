# ⚠️ WICHTIG: FALSCHER PORT!

## Problem erkannt! 🔍

Du rufst die App unter dem **falschen Port** auf:

```
❌ FALSCH: http://localhost:4301
✅ RICHTIG: http://localhost:4200
```

## Warum die CORS-Fehler auftreten:

1. **Angular Dev Server läuft auf Port 4200** (mit Proxy-Config)
2. **Du öffnest aber `localhost:4301`** im Browser
3. Dadurch wird der Proxy NICHT verwendet
4. Alle Requests gehen direkt an `http://192.168.178.25:4301`
5. → CORS-Fehler!

## Sofortige Lösung:

### 1. Öffne die App unter dem richtigen Port:
```
http://localhost:4200
```

### 2. Prüfe welche Server laufen:
```bash
npm run check-ports
```

oder direkt:
```bash
.\scripts\check-ports.bat
```

## Aktueller Status (aus netstat):

✅ Port 4200: Angular Dev Server läuft (Prozess 27508)  
✅ Port 3000: Backend Express (sollte laufen)  
✅ Port 3001: NestJS (sollte laufen)  

## Was passiert bei richtigem Port:

```
Browser → http://localhost:4200
           ↓ (Proxy aktiv!)
           /api/speech      → NestJS (localhost:3001)
           /api/transcripts → Backend (localhost:3000)
           /api/intent-logs → Backend (localhost:3000)
```

## Bookmark setzen:

**Speichere diesen Link als Lesezeichen:**
```
http://localhost:4200
```

❌ **NICHT:** `http://localhost:4301` oder `http://192.168.178.25:4301`

---

## Troubleshooting

### 404 bei http://localhost:4200

**Problem:** Dev-Server antwortet mit 404 Not Found

**Ursachen:**
1. Dev-Server ist abgestürzt
2. Dev-Server läuft nicht mehr
3. Alte Verbindungen blockieren Port

**Lösung:**
```bash
# 1. Stoppe alte Prozesse
Get-Process node | Where-Object {$_.Path -like "*raueberbude*"} | Stop-Process -Force

# 2. Starte Dev-Server neu
npm start

# 3. Warte ~15 Sekunden

# 4. Teste ob Server antwortet
Invoke-WebRequest http://localhost:4200
```

**Erwartete Ausgabe:**
```
StatusCode: 200
StatusDescription: OK
```

### CORS-Fehler trotz richtigem Port

**Prüfe:**
1. Browser ist wirklich auf `localhost:4200`? (nicht `localhost:4301`!)
2. Proxy-Config wird geladen? (Terminal-Output prüfen)
3. Backend-Server laufen? (`npm run check-ports`)

**Backend-Server starten:**
```bash
npm run start:dev
```

---

**Alle CORS-Probleme sind gelöst, wenn du den richtigen Port verwendest!** 🎉

